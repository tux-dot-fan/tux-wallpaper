# Core runtime dependencies
mpv >= 1.0
python-mpv >= 1.0
PyYAML >= 6.0

# Local service (FastAPI)
fastapi >= 0.100
pydantic >= 2.0
httpx >= 0.24
uvicorn[standard] >= 0.20

# System integration
pystray >= 0.19
Pillow >= 10.0
platformdirs >= 3.0

# Optional: gtk-layer-shell for proper Wayland support
# pip install gtk-layer-shell
# gtk-layer-shell >= 0.1

# Optional: web UI (pywebview)
# pip install webview
# webview >= 0.1
